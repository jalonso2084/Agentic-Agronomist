# Rule parameters
Hutton:
  min_temp_c: 10
  rh_threshold: 90
  min_hours_per_day: 6
  consecutive_days: 2

Smith:
  min_temp_c: 10
  rh_threshold: 90
  min_hours_per_day: 11
  consecutive_days: 2

# Example local rule to calibrate with agronomists (see memo).
LocalAndes:
  rh_threshold: 80
  min_temp_c: 15
  min_hours_per_day: 8
  consecutive_days_high: 2   # >=2 days -> High
  consecutive_days_mod: 1    # 1 day -> Moderate
